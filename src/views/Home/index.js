import React, { Component } from 'react';

export default class Home extends Component {

    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export const route = {
    path: '/',
    exact: true,
    label: 'Home',
    component: Home
};
